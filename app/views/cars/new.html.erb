<h1>New Car</h1>

<%= form_for @car do |f| %>
  <% if @car.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@car.errors.count, "error") %> prohibited
        this car from being saved:</h2>
      <ul>
        <% @car.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end%>
  <p>
    <%= f.label :color %>
    <%= f.text_field :color %>
  </p>

  <p>
    <%= f.label :mileage %>
    <%= f.text_field :mileage %>
  </p>

   <p>
    <%= f.label :year %>
    <%= f.text_field :year%>
  </p>

  <p>
    <%= f.label :description %>
    <%= f.text_field :description %>
  </p>

   <p>
    <%= f.collection_select(:manufacturer_id, @manufacturers, :id, :name) %>
  </p>

  <p>
    <%= f.submit "Submit Car"%>
  </p>
<% end %>

<%=link_to "Back", root_path %>
